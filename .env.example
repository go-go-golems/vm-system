# Optional absolute API base for the browser client.
# Leave empty to use relative /api/v1 and Vite dev proxy.
# Example: http://127.0.0.1:3210
VITE_VM_SYSTEM_API_BASE_URL=

# Session creation defaults used by the web UI.
# workspace_id/base_commit_oid are correlation metadata persisted with each session.
VITE_VM_SYSTEM_WORKSPACE_ID=ws-web-ui
VITE_VM_SYSTEM_BASE_COMMIT_OID=web-ui

# Absolute directory on the daemon host used as session worktree root.
# A worktree is the filesystem boundary for run-file and startup-file execution:
# - files are read from this directory
# - path traversal outside this root is rejected by the backend
# Use a dedicated folder for predictable behavior.
VITE_VM_SYSTEM_WORKTREE_PATH=/tmp/vm-system-ui-worktree

# Vite dev server proxy target for /api/v1 requests.
# Used when VITE_VM_SYSTEM_API_BASE_URL is empty.
VM_SYSTEM_API_PROXY_TARGET=http://127.0.0.1:3210
